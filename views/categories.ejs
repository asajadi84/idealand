<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="utf-8">
    <title>آیدیالند - دسته‌بندی‌ها</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style.css">
    <script src="js/jquery.min.js"></script>
    <script src="js/sweetalert2@11.js"></script>
</head>

<body>
    <nav class="header-nav">
        <div class="container">
            <div class="d-flex flex-row justify-content-between">
                <div class="d-flex flex-row">
                    <a href="/">
                        <div class="d-flex flex-row">
                            <img src="icons/logo.svg" alt="logo" class="nav-logo">
                            <h3 class="nav-title align-self-center">آیدیالند</h3>
                        </div>
                    </a>
                    <p class="nav-page-title align-self-center">دسته‌بندی‌ها</p>
                </div>

                <div class="align-self-center">
                    <a href="/addcategory" class="primary-link">
                        <img src="icons/add.svg" alt="icon" class="btn-icon">
                        افزودن دسته‌بندی</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="section-box">
            <div class="inner-container">
                <h3 class="main-title">دسته‌بندی‌ها</h3>
                <% if(!allCategories.length){ %>
                    <div class="alert alert-warning">هیچ دسته‌بندی‌ای وجود ندارد.</div>
                <% }else{ %>
                    <div class="table-responsive">
                        <table class="table">
                            <tr class="table-head">
                                <td>اولویت</td>
                                <td>کد</td>
                                <td>عنوان</td>
                                <td>ویرایش</td>
                            </tr>
                            <% for(let i = 0; i < allCategories.length; i++){ %>
                                <tr>
                                    <td><%= allCategories[i].category_priority %></td>
                                    <td><%= allCategories[i].category_code %></td>
                                    <td><%= allCategories[i].category_title %></td>
                                    <td>
                                        <a href="/editcategory?code=<%= allCategories[i].category_code %>" class="primary-link">
                                        <img src="icons/edit.svg" alt="icon" class="btn-icon">
                                        ویرایش</a>
                                    </td>
                                </tr>
                            <% } %>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="container">
            <div class="d-flex flex-row justify-content-between">
                <div>
                    <p class="footer-text">کپی رایت &copy; علی سجادی - 1401</p>
                </div>
                <div>
                    <p class="footer-text"><%= ideaCount %> ایده در <%= categoryCount %> دسته‌بندی</p>
                </div>
            </div>
        </div>
    </footer>

</body>

</html>